version: 2.1

jobs:
  analyze:
    docker:
      - image: 'cimg/openjdk:21.0.2'
    steps:
      - checkout
      - run:
          name: Analyze on SonarCloud
          command: mvn clean verify sonar:sonar -Dsonar.projectKey=dawncoll_sample-spring-boot-app -Dsonar.organization=dawncoll -Dsonar.login=b3420bf1ab87f29580e0de8cd0fdeb50e1ea1dd0

executors:
  jdk:
    docker:
      - image: 'cimg/openjdk:21.0.2'

orbs:
  maven: circleci/maven@1.4.1

workflows:
  maven_test:
    jobs:
      - maven/test:
          executor: jdk
      - analyze:
          context: SonarCloud
